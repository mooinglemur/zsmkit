MEMORY {
    JMPTBL0:    file = %O, start = $A000,  size = $0075, fill = yes, fillval = $00, bank = 0;
    SWITCH0:    file = %O, start = $A075,  size = $0003, fill = yes, fillval = $00, bank = 0;
    BANK0:      file = %O, start = $A078,  size = $1F88, fill = yes, fillval = $00, bank = 0;
    PADDING1:   file = %O, start = $A000,  size = $0075, fill = yes, fillval = $00, bank = 1;
    SWITCH1:    file = %O, start = $A075,  size = $0003, fill = yes, fillval = $00, bank = 1;
    BANK1:      file = %O, start = $A078,  size = $1F88, fill = no, bank = 1;
}

SEGMENTS {
    JMPTBL:         load = JMPTBL0,   type = ro;
    SWITCH0_TO_1:   load = SWITCH0,   type = ro;
    ZSMKIT0:        load = BANK0,     type = ro;
    ZSMKITBSS0:     load = BANK0,     type = bss, define = yes;
    ZSMKITBSS1:     load = PADDING1,  type = bss, define = yes;
    ZSMKIT1:        load = BANK1,     type = ro;
    ZSMKIT_LOWRAM:  load = BANK1,     type = ro, define = yes;
    SWITCH1_TO_0:   load = SWITCH1,   type = ro;
}

